<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta name="generator" content={Astro.generator} />

        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Space+Grotesk:wght@500;700&display=swap"
            rel="stylesheet"
        />

        <title>ProjectFRC Sri Lanka</title>
    </head>
    <body>
        <header class="site-header">
            <nav class="nav-container">
                <div class="logo">ProjectFRC LK</div>
                <ul class="nav-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/what-is-frc">What is FRC?</a></li>
                    <li><a href="/sponsors">Sponsor Us</a></li>
                </ul>
            </nav>
        </header>

        <main>
            <slot />
        </main>
    </body>
</html>

<style is:global>
    :root {
        --carbon: #141414;
        --card-bg: #242424;
        --cinnabar: #ab312d;
        --cinnabar-hover: #cf3d38;
        --white: #f8f9fa;
        --text-muted: #a0a09f;
        --cornflower: #3a6ea5;
        --steelazure: #004e98;
    }

    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        background: var(--carbon);
        color: var(--white);
        font-family: "Montserrat", sans-serif;
        line-height: 1.6;
    }

    h1,
    h2,
    h3 {
        font-family: "Space Grotesk", sans-serif;
        line-height: 1.2;
        margin-bottom: 0.75rem;
    }

    h1 {
        font-size: clamp(2rem, 4vw, 3.5rem);
    }

    main {
        width: 100%;
        max-width: 1250px;
        margin: 0 auto;
        padding: 2rem 1.5rem;
        display: flex;
        flex-direction: column;
        gap: 5rem;
    }

    ul {
        list-style-type: square;
        padding-left: 1rem;
    }

    a {
        text-decoration: underline dashed var(--text-muted) 0.5px;
        color: var(--text-muted);
    }

    a:hover {
        color: var(--cinnabar-hover);
        text-decoration-color: var(--cinnabar-hover)
    }

    .site-header {
        background-color: var(--card-bg);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        padding: 1rem 1.5rem;
    }

    .nav-container {
        display: flex;
        max-width: 1250px;
        justify-content: space-between;
        margin: 0 auto;
    }

    .logo {
        font-family: "Space Grotesk", sans-serif;
        font-weight: 700;
        font-size: 1.2rem;
    }

    .nav-links {
        display: flex;
        justify-content: space-between;
        gap: 1rem;
        list-style: none;
    }

    .nav-links a {
        text-decoration: none;
    }

    footer {
        text-align: center;
        padding: 2rem;
        color: var(--text-muted);
        font-size: 0.9rem;
    }

    .button-link {
        padding: 0.75rem 1.5rem;
        background-color: var(--cinnabar);
        color: var(--white);
        text-decoration: none;
        font-weight: 600;
        border-radius: 6px;
        display: inline-block;
        transition: background-color 0.2s ease;
        text-align: center;
    }

    .button-link:hover {
        background-color: var(--cinnabar-hover); color: var(--white);
    }

    .content-section {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .solid-bg {
        background-color: var(--card-bg);
        padding: 2rem;
        border-radius: 12px;
        border: 1px solid rgba(255, 255, 255, 0.05);
    }

    .subtitle {
        color: var(--text-muted);
        font-size: 1.1rem;
        margin-bottom: 2rem;
    }

    .card-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }

    .card {
        background-color: var(--card-bg);
        padding: 2rem;
        border-radius: 8px;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }
    
    .card p {
        color: var(--text-muted);
        font-size: 0.95rem;
        flex-grow: 1;
    }

    .outline-card {
        background-color: transparent;
        border: 1px solid var(--cornflower);
    }
</style>
